# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['attack_simulator.py'],
    pathex=[],
    binaries=[],
    datas=[('flask_app/firebase_config.json', '.'), ('attack_scripts/t1649.ps1', '.'), ('attack_scripts/t1574_011.ps1', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='attack_simulator',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
